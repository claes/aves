<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/audio_options"
        android:orderInCategory="100"
        android:showAsAction="always"
        android:title="@string/audio_options">
        <menu>
            <group
                android:id="@+id/audio_group"
                android:checkableBehavior="single" >
            <item
                android:id="@+id/audio_repeat"
                android:checkable="true"
                android:orderInCategory="100"
                android:showAsAction="always"
                android:title="@string/audio_repeat" />
            <item
                android:id="@+id/audio_autonext"
                android:checkable="true"
                android:orderInCategory="100"
                android:showAsAction="always"
                android:title="@string/audio_autonext" />
            </group>
        </menu>
    </item>
</menu>